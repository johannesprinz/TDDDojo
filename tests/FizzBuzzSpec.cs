using TDDDojo.Domain;
using FluentAssertions;
using Xunit;
using System;

namespace tests
{
    public class FizzBuzzSpec
    {
        // FizzBuzz is to print out number 1 to 100
        // replacing numbers divisible by 3 with Fizz
        // replacing numbers divisible by 5 with Buzz
        // replace numbers divicible by 3 and 5 with FizzBuzz

        [Fact]
        public void Lets_get_started()
        {
            throw new NotImplementedException();
        }
    }
}
